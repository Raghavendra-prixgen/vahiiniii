<odoo>

    <record id="view_fleet_transportation_order_form_invoice_details" model="ir.ui.view">
        <field name="name">transportation.order.invoice.details</field>
        <field name="model">transportation.order</field>
        <field name="inherit_id" ref="fleet_module_18.view_fleet_transportation_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//notebook/page[@name='y_transportation_order_intent_line_ids']"
                position="after">
                <page name="transportation_order_invoice_details" string="Invoice Details">
                    <field name="y_transportation_order_invoice_details_ids" widget="one2many"
                        nolabel="1" readonly="y_state == 'posted'">
                        <list name="transportation_order_invoice_details_list" string=" Lines"
                            editable="bottom">
                            <field name="y_transportation_order_id" column_invisible="1" />
                            <field name="y_transportation_order_intent_line_ids"
                                column_invisible="1" />
                            <field name="y_account_move_id" readonly="1" />
                            <field name="y_picking_ids" domain="[('state','=','assigned')]"
                                readonly="1" required="1" widget="many2many_tags" />
                            <field name="y_lr_number" />
                            <field name="y_indent_weight" readonly="1" sum="y_indent_weight" />
                            <field name="y_pod_document" widget="binary"
                                filename="y_attachment_filename" />
                            <field name="y_attachment_date" />
                            <field name="y_attachment_filename" column_invisible="1" />
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <record id="purchase_order_view_tree_fleet_module" model="ir.ui.view">
        <field name="name">purchase.order.view.list.fleet.module</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <list decoration-muted="state == 'cancel'"
                decoration-info="invoice_status == 'to invoice'"
                string="Purchase Order"
                class="o_purchase_order"
                sample="1">
                <header>
                    <button name="button_fleet_get_pickings" type="object" string="Process" />
                </header>
                <field name="priority" optional="show" widget="priority" nolabel="1" />
                <field name="partner_ref" optional="hide" />
                <field name="name" string="Reference" readonly="1" decoration-bf="1"
                    decoration-info="state in ('draft','sent')" />
                <field name="date_approve" column_invisible="context.get('quotation_only', False)"
                    optional="show" />
                <field name="partner_id" readonly="state in ['cancel', 'done', 'purchase']" />
                <field name="company_id" groups="base.group_multi_company"
                    options="{'no_create': True}" optional="show"
                    readonly="state in ['cancel', 'done', 'purchase']" />
                <field name="company_id" groups="!base.group_multi_company" column_invisible="True"
                    readonly="state in ['cancel', 'done', 'purchase']" />
                <field name="user_id" widget="many2one_avatar_user" optional="show" />
                <field name="date_order" column_invisible="not context.get('quotation_only', False)"
                    readonly="state in ['cancel', 'done', 'purchase']" optional="show" />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field name="origin" optional="show" />
                <field name="amount_untaxed" sum="Total Untaxed amount" string="Untaxed"
                    widget="monetary" optional="hide" />
                <field name="amount_total" sum="Total amount" widget="monetary" optional="show"
                    decoration-bf="1" />
                <field name="currency_id" column_invisible="True"
                    readonly="state in ['cancel', 'done', 'purchase']" />
                <field name="state" column_invisible="True" />
                <field name="amount_total_cc" sum="Total amount" widget="monetary" optional="hide" />
                <field name="company_currency_id" column_invisible="True" />
                <field name="invoice_status" widget="badge"
                    decoration-success="invoice_status == 'invoiced'"
                    decoration-info="invoice_status == 'to invoice'" optional="show" />
                <field name="date_planned" column_invisible="context.get('quotation_only', False)"
                    optional="show" />
            </list>
        </field>
    </record>

    <record id="ir_action_purchase_order_view_fleet_module" model="ir.actions.server">
        <field name="name">Get Purchase Orders</field>
        <field name="model_id" ref="model_transportation_order" />
        <field name="binding_model_id" ref="model_transportation_order" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            if records:
            action = records.get_purchase_orders()
        </field>
    </record>

    <record id="account_move_view_tree_fleet_module" model="ir.ui.view">
        <field name="name">account.invoice.list</field>
        <field name="model">account.move</field>
        <field name="arch" type="xml">
            <list string="Invoices" decoration-info="state == 'draft'"
                decoration-muted="state == 'cancel'"
                expand="context.get('expand', False)"
                sample="1">
                <header>
                    <button name="button_fleet_get_pickings" type="object" string="Process" />
                </header>
                <field name="made_sequence_gap" column_invisible="True" />
                <field name="duplicated_ref_ids" column_invisible="True" />  <!-- Needed for custome
                view account_tree-->
                <field name="name" decoration-bf="1"
                    decoration-danger="made_sequence_gap and state == 'posted'"
                    widget="char_with_placeholder_field" placeholder="/" />
                <field name="invoice_partner_display_name"
                    column_invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund', 'in_receipt')"
                    groups="base.group_user" string="Vendor" />
                <field name="invoice_partner_display_name"
                    column_invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt')"
                    groups="base.group_user" string="Customer" />
                <field name="invoice_date" optional="show"
                    column_invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund', 'in_receipt')"
                    readonly="state != 'draft'" string="Bill Date"
                    decoration-warning="abnormal_date_warning" />
                <field name="invoice_date" optional="show"
                    column_invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt')"
                    readonly="state != 'draft'" string="Invoice Date"
                    decoration-warning="abnormal_date_warning" />
                <field name="date" optional="hide" string="Accounting Date"
                    readonly="state in ['cancel', 'posted']" />
                <field name="invoice_date_due" widget="remaining_days" optional="show"
                    invisible="payment_state in ('paid', 'in_payment', 'reversed') or state == 'cancel'" />
                <field name="invoice_origin" optional="hide" string="Source Document" />
                <field name="payment_reference" optional="hide"
                    column_invisible="context.get('default_move_type') in ('out_invoice', 'out_refund', 'out_receipt')" />
                <field name="ref" optional="hide" />
                <field name="invoice_user_id" optional="hide"
                    column_invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt')"
                    string="Salesperson" widget="many2one_avatar_user" />
                <field name="activity_ids" widget="list_activity" optional="hide" />
                <field name="company_id" groups="base.group_multi_company"
                    options="{'no_create': True}" optional="hide" />
                <field name="company_id" groups="!base.group_multi_company" column_invisible="True" />
                <field name="amount_untaxed_in_currency_signed" string="Tax Excluded" sum="Total"
                    optional="show" />
                <field name="amount_tax_signed" string="Tax" sum="Total" optional="hide" />
                <field name="amount_total_in_currency_signed" string="Total" sum="Total"
                    optional="show" decoration-bf="1" decoration-warning="abnormal_amount_warning"
                    groups="base.group_multi_currency" />
                <field name="amount_total_in_currency_signed" string="Total" sum="Total"
                    optional="hide" decoration-bf="1" decoration-warning="abnormal_amount_warning"
                    groups="!base.group_multi_currency" />
                <field name="amount_residual_signed" string="Amount Due" sum="Amount Due"
                    optional="hide" />
                <field name="currency_id" optional="hide" readonly="state in ['cancel', 'posted']" />
                <field name="company_currency_id" column_invisible="True" />
                <field name="checked" optional="hide" widget="boolean_toggle" />
                <field name="status_in_payment"
                    string="Status"
                    widget="badge"
                    decoration-info="state == 'draft'"
                    decoration-danger="payment_state == 'not_paid' and invoice_date_due &lt; context_today().strftime('%Y-%m-%d')"
                    decoration-warning="payment_state in ('partial', 'in_payment')"
                    decoration-success="payment_state in ('paid', 'reversed')"
                    invisible="payment_state == 'invoicing_legacy' or move_type == 'entry'"
                    optional="show"
                />
                <field name="move_sent_values"
                    string="Sent"
                    widget="badge"
                    decoration-success="move_sent_values == 'sent'"
                    decoration-danger="move_sent_values == 'not_sent'"
                    column_invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt')"
                    optional="hide"
                />
                <field name="move_type" column_invisible="context.get('default_move_type', True)" />
                <field name="abnormal_amount_warning" column_invisible="1" />
                <field name="abnormal_date_warning" column_invisible="1" />
            </list>
        </field>
    </record>

    <record id="ir_action_account_move_view_fleet_module" model="ir.actions.server">
        <field name="name">Get Invoices</field>
        <field name="model_id" ref="model_transportation_order" />
        <field name="binding_model_id" ref="model_transportation_order" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            if records:
            action = records.get_invoices()
        </field>
    </record>


</odoo>