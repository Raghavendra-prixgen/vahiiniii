<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="custom_contact_child_vat_number" model="ir.ui.view">
        <field name="name">child.contact.vat.number</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook/page/field[@name='child_ids']/form//field[@name='mobile']" position="after">
                <field name="vat" invisible="type not in ['invoice','delivery']"/>
            </xpath>
        </field>
    </record>

    <record id="invoice_form_l10n_in_gstin" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="invoice_vendor_bill_id" position="after">
                <field name="l10n_in_gstin" readonly="1" invisible="state != 'posted' or move_type != 'out_invoice'"/>
            </field>
        </field>
    </record>

    <record id="view_partner_form_inherit_new" model="ir.ui.view">
        <field name="name">res.partner.inherit.new</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="attributes">
                <attribute name="readonly">parent_id and type != 'delivery'</attribute>
            </xpath>
        </field>
    </record>


</odoo>
