
<odoo>
    <record id="new_journal_entry_domain" model="ir.actions.act_window">
        <field name="name">Direct Expense / Income voucher</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('y_is_direct_expense_income_voucher','=',True)]</field>
        <field name="context">{'new_jv' : 'new_jv','y_is_direct_expense_income_voucher':True}</field>
        <field name="target">current</field>
    </record>


 
        <record id="account.action_move_journal_line" model="ir.actions.act_window">
            <field name="domain">[('y_is_direct_expense_income_voucher','=',False)]</field>
          
        </record>

    
        <menuitem id="new_journal_entry_child"
                    name="Direct Expense / Income voucher"
                    action="new_journal_entry_domain"
                    parent="account.menu_finance_entries"
                   />

</odoo>