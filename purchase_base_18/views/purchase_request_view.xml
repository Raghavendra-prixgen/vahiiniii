<?xml version="1.0" ?>
<odoo>
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <button
                        name="button_draft"
                        invisible="state not in ('to_approve', 'approved', 'rejected')"
                        string="Reset"
                        type="object"
                        groups="purchase_base_18.group_purchase_request_manager"
                    />

                    <button
                        name="button_to_approve"
                        invisible="state != 'draft'"
                        string="Request for Approval"
                        type="object"
                        class="oe_highlight"
                    />
                    <button
                        name="button_approved"
                        invisible="state != 'to_approve'"
                        string="Approve"
                        type="object"
                        class="oe_highlight"
                        groups="purchase_base_18.group_purchase_request_manager"
                    />
                    <button
                        name="%(action_purchase_request_line_make_purchase_order)d"
                        invisible="state not in ('approved','partial')"
                        string="Create RFQ"
                        type="action"
                    />
                    <button
                        name="button_done"
                        invisible="True"
                        string="Done"
                        type="object"
                        class="oe_highlight"
                        groups="purchase_base_18.group_purchase_request_manager"
                    />
                    <button
                        name="button_rejected"
                        invisible="state not in ('to_approve')"
                        string="Reject"
                        type="object"
                        groups="purchase_base_18.group_purchase_request_manager"
                    />
                    <button
                        name="button_cancel"
                        invisible="state in ('cancel', 'approved','done','rejected')"
                        string="Cancel"
                        type="object"
                    />
                    
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,to_approve,approved,done"
                        statusbar_colors='{"approved":"blue"}'
                    />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            type="object"
                            name="action_view_purchase_request_line"
                            class="oe_stat_button"
                            invisible="line_count == 0"
                            icon="fa-list"
                        >
                            <field name="line_count" widget="statinfo" string="Lines" />
                        </button>
                        <button
                            type="object"
                            name="action_view_purchase_order"
                            class="oe_stat_button"
                            invisible="purchase_count == 0"
                            icon="fa-shopping-cart"
                            string="Purchase Order"
                        >
                            <field
                                name="purchase_count"
                                widget="statinfo"
                                string="Purchase Orders"
                            />
                        </button>
                            


                    </div>
                    <div class="oe_edit_only">
                        <label for="name" class="oe_inline" />
                    </div>
                    <h1>
                        <field name="is_editable" invisible="1" />
                        <field name="name" class="oe_inline" readonly="1"/>
                    </h1>
                    <group>
                        <group>
                            <field name="parent_company_id" invisible="1"/>
                            <field name="request_type_id" options="{'no_create': 1, 'no_edit': 1, 'no_open': 1}" required="1" domain="['|','|','|',('company_id', '=', False),('company_id', 'child_of', company_id),('company_id', '=', company_id),('company_id', '=', parent_company_id)]" readonly="not is_editable"/>
                            <field name="assigned_to" readonly="not is_editable" invisible="1"/>
                            <field name="company_id" widget="selection" readonly="not is_editable" required="1"/>
                            <field name="picking_type_id" readonly="not is_editable" options="{'no_create': True, 'no_create_edit':True, 'no_open': True}"/>
                        </group>
                        <group>
                            <field name="create_date" widget="date" string="Create Date"/>
                            <field name="origin" class="oe_inline" readonly="1"
                            />
                            <field name="requested_by" readonly="not is_editable" options="{'no_create': True, 'no_create_edit':True, 'no_open': True}"/>
                            <field name="approver_ids" widget="many2many_tags" force_save="1" readonly="1"/>
                        </group>
                        
                    </group>
                    <notebook>
                        <page string="Products">
                            <field name="line_ids" readonly="state == 'approved'">
                                <list decoration-muted="cancelled == True" editable="bottom">
                                    <field name="product_id" required="1" options="{'no_create': True, 'no_create_edit':True, 'no_open': True}"/>
                                    <field name="name"/>
                                    <field name="product_qty"/>
                                    <field name="product_uom_category_id" column_invisible="True"
                                    />
                                    <field name="product_uom_id" column_invisible="True" groups="!uom.group_uom"/>
                                    <field name="product_uom_id" groups="uom.group_uom"
                                    />
                                    <field name="forecasted_issue" column_invisible="True"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report" icon="fa-area-chart" invisible="not id or not forecasted_issue or not product_id.is_storable" class="text-danger"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report" icon="fa-area-chart" invisible="not id or forecasted_issue or not product_id.is_storable"/>

                                    <field name="analytic_distribution" widget="analytic_distribution" groups="analytic.group_analytic_accounting" options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}" optional="hide"
                                    />
                                    <field name="date_required" />
                                    <field name="estimated_cost" widget="monetary" column_invisible="True"/>
                                    <field name="currency_id" column_invisible="True" />
                                    <field name="company_id" groups="base.group_multi_company"
                                        widget="selection" column_invisible="True"/>
                                    <field name="cancelled" column_invisible="True" />
                                    <field name="is_editable" column_invisible="True" />
                                    <field name="purchased_qty" />
                                    <field name="purchase_state" widget="badge"
                                        decoration-success="purchase_state == ('done')"
                                        decoration-muted="purchase_state == ('draft')"
                                        decoration-info="purchase_state in ('sent', 'purchase')"
                                        decoration-warning="purchase_state == ('to_approve')"
                                        decoration-danger="purchase_state == 'cancelled'"
                                    />
                                    <button name="action_show_details" type="object" icon="fa-list"   title="Show Details" width="0.1" options='{"warn": true}'    column_invisible="True"
                                    />
                                    <field name="y_is_short_closed" readonly="1" optional="hide"/>
                                    <field name="y_short_close_reason_id" force_save="1" readonly="1" optional="hide"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <record id="view_purchase_request_tree" model="ir.ui.view">
        <field name="name">purchase.request.list</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list
                decoration-bf="message_needaction==True"
                decoration-info="state in ('draft','to_approve')"
                decoration-muted="state in ('rejected')"
                name="Purchase Request"
            >
                <field name="message_needaction" column_invisible="True"/>
                <field name="name" />
                <field name="create_date" string="Create Date" widget="date"/>
                <field name="requested_by" widget="many2one_avatar_user" />
                <field
                    name="company_id"
                    groups="base.group_multi_company"
                    widget="selection"
                />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field name="origin" />
                <field name="currency_id" column_invisible="True" />
                <field name="estimated_cost" optional="hide" />
                <field
                    name="state"
                    widget="badge"
                    decoration-success="state in ('done', 'approved','partial')"
                    decoration-muted="state == 'draft'"
                    decoration-warning="state == 'to_approve'"
                    decoration-danger="state == 'rejected'"
                />
                <field name="y_is_short_closed" optional="hide"/>
            </list>
        </field>
    </record>
    <record id="view_purchase_request_search" model="ir.ui.view">
        <field name="name">purchase.request.list.select</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <search string="Search Purchase Request">
                <field name="name" string="Purchase Request"/>
                <field name="product_id" />
                <field name="state"/>
                <filter
                    name="unassigned"
                    string="Unassigned"
                    domain="[('assigned_to','=', False)]"
                    help="Unassigned Request"
                />
                <separator />
                <filter
                    name="state_draft"
                    string="Draft"
                    domain="[('state','=','draft')]"
                    help="Request is to be approved"
                />
                <filter
                    name="state_to_approve"
                    string="To Approve"
                    domain="[('state','=','to_approve')]"
                    help="Request is to be approved"
                />
                <filter
                    name="state_approved"
                    string="Approved"
                    domain="[('state','=','approved')]"
                    help="Request is approved"
                />
                <filter
                    name="state_rejected"
                    string="Rejected"
                    domain="[('state','=','rejected')]"
                    help="Request is rejected"
                />
                <filter
                    name="state_done"
                    string="Done"
                    domain="[('state','=','done')]"
                    help="Request is done"
                />
                <separator />
                <filter
                    string="Unread Messages"
                    name="message_needaction"
                    domain="[('message_needaction','=',True)]"
                />
                <filter
                    name="assigned_to_me"
                    domain="[('assigned_to','=', uid)]"
                    help="Assigned to me"
                />
                <filter
                    name="my_requests"
                    domain="[('requested_by','=', uid)]"
                    help="My requests"
                />
                <field name="requested_by" />
                <field name="assigned_to" />
                <group expand="0" string="Group By...">
                    <filter
                        name="requested_by"
                        string="Requested by"
                        icon="fa-user"
                        domain="[]"
                        context="{'group_by':'requested_by'}"
                    />
                    <filter
                        name="assigned_to"
                        string="Assigned to"
                        icon="fa-user"
                        domain="[]"
                        context="{'group_by':'assigned_to'}"
                    />
                    <filter
                        name="source"
                        string="Source"
                        icon="fa-caret-square-o-left"
                        domain="[]"
                        context="{'group_by':'origin'}"
                    />
                    <filter
                        name="start_date"
                        string="Start Date"
                        icon="fa-calendar"
                        domain="[]"
                        context="{'group_by':'create_date'}"
                    />
                </group>
            </search>
        </field>
    </record>
    <record model="ir.actions.act_window" id="purchase_request_form_action">
        <field name="name">Purchase Requests</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,form</field>
        <field name="context">{"search_default_requested_by":uid}</field>
        <field name="search_view_id" ref="view_purchase_request_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No Purchase Request found. Let's create one!
            </p>
        </field>
    </record>
    
    <menuitem
        id="menu_purchase_request_act"
        sequence="10"
        parent="purchase.menu_procurement_management"
        action="purchase_request_form_action"
    />
</odoo>
